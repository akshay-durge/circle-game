<%= form_for(@notice_board) do |f| %>
  <% if @notice_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notice_board.errors.count, "error") %> prohibited this notice_board from being saved:</h2>

      <ul>
      <% @notice_board.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
